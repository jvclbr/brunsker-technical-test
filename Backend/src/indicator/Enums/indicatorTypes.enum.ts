import * as dbConstants from 'db.constants.js';

export enum IndicatorTypesEnum{
    ROLE = dbConstants.indicatorsTypes.ROLE,
    UF = dbConstants.indicatorsTypes.UF,
    LOCALITY = dbConstants.indicatorsTypes.LOCALITY,
    REAL_ESTATE_TYPE = dbConstants.indicatorsTypes.REAL_ESTATE_TYPE,
}